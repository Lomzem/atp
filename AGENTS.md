## Project Rules

- Do not put any identifying information in the code/docs (e.g. from example
  build logs, .elf files, or project trees). The log fixtures in
  tests/fixtures/logs are sanitised copies of real builds; keep them that way.
